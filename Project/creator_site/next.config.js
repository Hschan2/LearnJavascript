/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['raw.githubusercontent.com', 'img1.daumcdn.net', 'img.youtube.com', 'i.ytimg.com'],
    },
}

module.exports = nextConfig
